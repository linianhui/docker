version: '3'

services:

  portainer:
    build: ./portainer/
    restart: on-failure
    command: -H unix:///var/run/docker.sock
    networks:
      - network_default
    ports:
      - "9000:9000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - volume_portainer:/data

networks:
  network_default:
    driver: bridge

volumes:
  volume_portainer:
