version: '3'


networks:
  network_default:
    driver: bridge


services:

  jenkins:
    build: ./jenkins/
    networks:
      - network_default
    ports:
      - "8080:8080"
      - "50000:50000"
    volumes:
      - volume_jenkins:/var/jenkins_home


volumes: 
  volume_jenkins: